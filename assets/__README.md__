
imgcook 示例项目，参考 create-react-app。可在本地预览 react-standard dsl 生成的代码。

## 启动

`yarn start`

打开 [http://localhost:3000](http://localhost:3000) 查看您的示例。 

如果您对源文件有改动页面将会自动刷新

## 获取模块

在项目根目录下执行 `imgcook pull <moduleid>`

完成后，即可在 [http://localhost:3000](http://localhost:3000) 查看结果。
